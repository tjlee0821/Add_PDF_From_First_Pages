# Claude Notes

Follow `AGENTS.md` and `PROJECT_RULES.md`.

This project prioritizes production compatibility over redesign. Do not introduce new architecture for Stage 2 unless explicitly requested.

When working on exports, preserve the legacy column order and leave the original `target_name` column position blank after `replaced_name`.

